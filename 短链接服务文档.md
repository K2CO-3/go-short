# GoShort 短链接服务文档

基于 Go 的微服务短链接系统，包含**跳转、管理 API、异步日志 Worker** 三个独立后端服务，以及 **Vite + React** 管理前端。访问日志经 **Kafka** 异步落库（Redirect 生产、Worker 消费写入 PostgreSQL）。

---

## 1. 项目结构

```
go-short/
├── cmd/
│   ├── api-server/         # API：用户、短链、管理后台
│   ├── redirect-server/   # 跳转：302 重定向，读流量与缓存核心
│   └── worker/            # 消费 Kafka 访问日志，写入 PostgreSQL
├── frontend/               # Web 管理端（Vite、React、TypeScript）
│   ├── package.json
│   └── package-lock.json  # 前端依赖锁定（见 README 说明）
├── internal/
│   ├── bloom/             # 布隆过滤器，减轻 Redis/DB 穿透
│   ├── handler/            # HTTP（auth, link, user, admin）与校验（validator）
│   ├── middleware/         # 鉴权、管理员中间件
│   ├── model/              # 数据模型（User, Link, AccessLog 等）
│   ├── mq/                 # Kafka 访问日志读写封装
│   ├── repository/         # 数据访问（PostgreSQL、Redis 等实现）
│   ├── service/            # 业务逻辑
│   ├── util/               # 短码、JWT、密码等工具
│   └── …
├── deploy/
│   ├── docker-compose.yml
│   ├── nginx/              # 反代、静态与路由
│   └── Dockerfile.*
├── go.mod
├── README.md
└── 短链接服务文档.md
```

---

## 2. 服务划分（与 compose 对应）

| 服务 | 职责 | 对外端口（宿主机，默认 compose） |
|------|------|----------------------------------|
| **Nginx** | 反代 80/443，转发 API / 跳转 / 前端 | 80、443 |
| **redirect-service** | 短链 302 跳转，本地缓存 + Redis + 布隆，异步写访问日志到 Kafka | 8082（映射容器 8080） |
| **api-service** | 注册/登录、短链与资料、管理端 JSON API | 8081（映射容器 8080） |
| **worker-service** | 消费 Kafka 访问日志，写入 PostgreSQL | 无 |
| **frontend-service** | 构建产出的静态管理界面，由 Nginx 转发 | 经 Nginx 对外 |
| **postgres / redis / kafka** | 存储与消息队列 | 5432 / 6379 / 9092 |

> 单进程调试时，各 `main` 内监听端口以代码/环境变量为准；上表为 `deploy/docker-compose.yml` 的常用映射。

---

## 3. 数据模型（摘要）

### 3.1 Users

- `id`（UUID）、`username`、`password_hash`、`email`（可空）
- `role`：`user` / `admin`
- `status`：如 `active` / `inactive`（与业务「启用/禁用用户」一致）
- `link_count` 等统计字段

### 3.2 Links

- `id`、`short_code`（唯一）、`original_url`、`alias`
- `user_id`（UUID）、`is_custom`、`visit_count`
- `expires_at`（可空）、`status`（布尔，表示短链是否启用）
- 索引与业务查询见 `internal/repository`

### 3.3 AccessLogs

- `link_id`、`short_code`、`ip_address`、`user_agent`、`visited_at`
- 由 Redirect 发 Kafka，Worker 消费后写入

---

## 4. 跳转链路（Redirect 服务）

```
请求 GET /code/:code
    ├─ 本地 LRU 等缓存命中 → 302
    ├─ 未命中 → 布隆：判定极不可能存在 → 404
    ├─ 可能命中 → 查 Redis → 命中则回填并 302
    └─ 再查 PostgreSQL；命中后回写各级缓存，并 302；否则 404
```

- 删除/禁用短链时，API 会触发**缓存失效**（含 Redis 及本地缓存，具体见 `CacheInvalidator` 与相关 Worker）。
- 成功跳转后，访问记录**异步**写入 Kafka，由 **Worker** 落库。

---

## 5. 异步访问日志（Kafka）

- Redirect 使用 `kafka-go` 将访问记录写入配置的 Broker（如 `KAFKA_BROKERS`）。
- Worker 以 Consumer Group 消费，仅处理成功后再提交 offset，偏 **at-least-once** 语义，失败可重试。

---

## 6. API 概览

Base 路径以 **`/api/v1`** 为前缀（由网关或 `main` 中路由决定）。

### 6.1 认证

- `POST /auth/register`：注册
- `POST /auth/login`：登录，返回 JWT 及 `role`（`user` / `admin`）

### 6.2 短链（需 `Authorization: Bearer <token>`）

- `POST /links`：创建短链（`url` 等经服务端与校验层校验；无协议会补全 `https://`）
- `GET /links`：我的短链列表（分页）
- `GET /links/GetLinksByAlias`：按别名筛选
- `PUT /links/:id/activate`、`PUT /links/:id/unactivate`：启用/禁用（**属主**或 **admin**）
- `DELETE /links/:id`：删除（属主或 admin）

### 6.3 用户

- `GET /user/profile`：个人资料（含 `role`）
- `PUT /user/profile`：更新资料
- `PUT /user/password`：改密

### 6.4 管理员（需 JWT 中 `admin` 角色，且走管理员中间件）

- `POST /admin/createUser`
- `GET /admin/getUserList`：用户列表（含 `role`）
- `GET /admin/getLinkList`：全站短链列表
- `DELETE /admin/delete/:userID`：删用户
- `PUT /admin/unactivateUser/:userID`、`PUT /admin/activateUser/:userID`
- `PUT /admin/unactivateLink/:linkID`、`PUT /admin/activateLink/:linkID`
- `GET /admin/logs`：访问日志，查询参数如 `start_time`、`end_time`、`ip_address`、`short_code`、`original_url`、`limit`（时间格式与后台解析见 `admin_handler` 的 `parseAccessLogTime`）

---

## 7. 前端（`frontend/`）

- **技术栈**：Vite 5、React 18、React Router 6、TypeScript。
- **能力**：注册/登录、短链创建与启停/删除、个人中心、**管理员**用户/全站短链/访问日志筛选等；构建产物由 Docker 中 **frontend 镜像** 提供，Nginx 对外访问。
- **本地开发**：`cd frontend && npm run dev`（依赖安装见 `package.json` / `package-lock.json`）。

---

## 8. 部署

```bash
cd deploy
docker-compose up -d
```

常见环境变量示例：`DB_DSN`、`REDIS_ADDR`、`KAFKA_BROKERS`、`JWT_SECRET`、`BASE_URL` 等，见各 `Dockerfile` 与 `docker-compose` 中 `environment`。

---

## 9. 技术栈（后端）

- **Web**：Gin  
- **ORM**：GORM  
- **数据库**：PostgreSQL  
- **缓存与辅助**：Redis（缓存、Pub/Sub 等，见仓库实现）  
- **消息队列**：Kafka（访问日志）  
- **认证**：JWT  
- **其它**：布隆防穿透、本地 LRU、校验

---
